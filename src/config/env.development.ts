
// src/config/env.development.ts
import { SupabaseConfig } from './environments';

const config: SupabaseConfig = {
  supabaseUrl: 'YOUR_DEV_SUPABASE_URL', // TODO: Replace with your actual DEV Supabase URL
  supabaseAnonKey: 'YOUR_DEV_SUPABASE_ANON_KEY', // TODO: Replace with your actual DEV Supabase Anon Key
};

export default config;
